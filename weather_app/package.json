{
  "name": "api",
  "version": "1.0.0",
  "description": "create a basic weather application using free api and intro with json",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hasibullah Aman",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
